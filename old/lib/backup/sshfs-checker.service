[Unit]
Description=Maintains sshfs connections established by fstab
Requires=network.target
After=network.target

[Service]
Type=simple
ExecStart=/bin/bash /scripts/sshfs-checker.bsh

[Install]
WantedBy=multi-user.target
Alias=sshfs-checker
